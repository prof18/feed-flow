import android.app.Activity

@Suppress("UnusedParameter")
object PlayReviewManager {
    fun triggerReviewFlow(
        activity: Activity,
        onReviewDone: () -> Unit,
    ) {
        // no-op
    }
}
