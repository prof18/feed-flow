import SwiftUI
import UIKit

enum ReaderTheme {
    // for body text
    static var foreground: UIColor = .label
    // used for button titles
    static var foreground2: UIColor = .secondaryLabel
    // page background
    static var background: UIColor = .systemBackground
    // used for buttons
    static var background2: UIColor = .secondarySystemBackground
    static var link: UIColor = .systemBlue
}
